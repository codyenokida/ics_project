#ifndef DEBUGGER_HPP
#define DEBUGGER_HPP

#include "HashMap.hpp"
#include <string>
#include <iostream>


void debugOn(std::string* array, HashMap* hash);

void loginCount(std::string* array, HashMap* hash);

void bucketCount(std::string* array, HashMap* hash);

void loadFactor(std::string* array, HashMap* hash);

#endif
